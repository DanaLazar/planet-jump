export type Planet = {
    name: string;
    temperature: number;
    distance_light_year: string;
};

export type Step = {
    step: number;
}

export type PersonalInfo = {
    name: string;
    greeting: string;
    image: string;
};